<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhinengshe.dao.shirouser.ShiroUserMapper">
	<resultMap id="BaseResultMap" type="com.zhinengshe.pojo.shirouser.ShiroUser">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="userName" property="username" jdbcType="VARCHAR" />
		<result column="password" property="password" jdbcType="VARCHAR" />
		<result column="roleId" property="roleid" jdbcType="INTEGER" />
	</resultMap>
	
	<sql id="Base_Column_List">
		id, userName, password, roleId
	</sql>

	<select id="findUserByUsername" parameterType="String"
		resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from shiro_user where userName=#{username}
	</select>

	<select id="findRoles" parameterType="String" resultType="String">
		select r.roleName from shiro_user u,shiro_role r where u.roleId=r.id and
		u.userName=#{username}
	</select>

	<select id="findPermissions" parameterType="String" resultType="String">
		select p.permissionName from shiro_user u,shiro_role r,shiro_permission p
		where u.roleId=r.id and p.roleId=r.id and u.userName=#{username}
	</select>



</mapper>